# Core ML Libraries for macOS (CPU/MPS)
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# HuggingFace Transformers (v4.35.0 for PyTorch 2.2.2 compatibility)
transformers==4.35.0
tokenizers==0.14.1
huggingface-hub==0.17.3
# Note: accelerate removed due to dependency conflicts with older huggingface-hub

# Image Processing
opencv-python==4.11.0.86
Pillow==11.3.0

# Utilities
numpy==1.26.4
protobuf==6.33.0
psutil==7.1.3

# For better performance
safetensors==0.6.2

# Additional dependencies for the project
typing-extensions>=4.0.0

# Configuration and HTTP
PyYAML>=6.0.0
requests>=2.28.0

